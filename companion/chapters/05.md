# Chapter 5: SQL, NoSQL, and Product Reality

## Product question

> What records, relationships, grain, and denominator must a query preserve to support the intended product decision?

## What the agent brings into chat

1. [Exact SQL, syntax explanation, result, and limitation](../sql/sakila/README.md)
2. [Relational-grain figure](../figures/png/figure_5_1.png)
3. [Product Data Question Record](../templates/product_data_question.md)

The agent renders the SQL and captured result in chat, then runs the bounded SQLite exercise itself and walks through the live output.

## Broader SQL progression

Chapter 5 contains eight executable examples, not only the rentals-per-copy investigation. Read them in this order:

1. `SELECT`, `WHERE`, `ORDER BY`, and `LIMIT` for PG-rated films.
2. `COUNT(*)` and `GROUP BY` for catalog composition by rating.
3. `JOIN` across customer, rental, inventory, and film records.
4. A common table expression that normalizes rentals by inventory copies.
5. `SUM`, a half-open date range, and store-level revenue.
6. `COUNT(DISTINCT ...)` for inventory on open rental at a historical cutoff.
7. A common table expression plus subquery for above-average customer spend.
8. `CASE` for explicit analytical categories.

The complete runnable file is [chapter_5_examples.sql](../sql/sakila/queries/chapter_5_examples.sql). Each pattern answers a different kind of product question and carries a different interpretation risk.

## Five-minute walkthrough

1. State the product decision before reading the query.
2. Identify the output grain: one film per row.
3. Trace the `film → inventory → rental` join path.
4. Explain why recorded rentals are divided by distinct inventory copies.
5. Compare the worked investigation with at least two examples in the broader progression.
6. State why the result can prioritize an investigation but cannot prove unmet demand.

## Take it to your product

Complete one Product Data Question Record before requesting a dashboard or query. Name the decision, metric, grain, sources, relationships, validation, and interpretation boundary.

## Ask an AI coding agent

> Render the exact SQL and a compact result table directly in chat. Explain the question, grain, numerator, denominator, joins, filters, ordering, result, and limitation. Run the bounded SQLite exercise yourself and walk me through the output. Then ask me to change one product assumption in plain language before proposing a different query.

## Agent-executed evidence

The agent runs the bounded check and displays the relevant output in chat:

```sh
./companion/sql/validate_chapter_05.sh
```

When the broader progression helps the lesson, the agent can also print all eight book examples:

```sh
./companion/sql/sakila/run_chapter_examples.sh
```

## Evidence boundary

A syntactically valid query can still answer the wrong product question. The supplied result is evidence about the open-licensed Sakila dataset, not a production benchmark or causal finding.
