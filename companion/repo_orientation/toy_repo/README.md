# Noted cover images

This fictional feature allows a user to add, replace, or remove a document cover image. Product documentation lives in `docs/design/uploads.md`; implementation and tests live under `src/` and `tests/`.

The repository intentionally contains one unresolved product-contract contradiction for the Chapter 16 exercise.
